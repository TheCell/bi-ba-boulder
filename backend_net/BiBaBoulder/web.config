<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Disable WebDAV for API routes to allow PUT/DELETE verbs -->
  <location path="api">
    <system.webServer>
      <modules>
        <remove name="WebDAVModule" />
      </modules>
      <handlers>
        <remove name="WebDAV" />
      </handlers>
    </system.webServer>
  </location>

  <!-- Disable WebDAV for user content uploads (managed via API) -->
  <location path="filestorage">
    <system.webServer>
      <modules>
        <remove name="WebDAVModule" />
      </modules>
      <handlers>
        <remove name="WebDAV" />
      </handlers>
    </system.webServer>
  </location>

  <!-- Main application configuration -->
  <location path="." inheritInChildApplications="false">
    <system.webServer>
      <handlers>
        <add name="aspNetCore" path="*" verb="*" modules="AspNetCoreModuleV2" resourceType="Unspecified" />
      </handlers>
      <aspNetCore processPath="dotnet" 
                  arguments=".\Thecell.Bibaboulder.BiBaBoulder.dll" 
                  stdoutLogEnabled="true" 
                  stdoutLogFile=".\logs\stdout" 
                  hostingModel="outofprocess">
        <environmentVariables>
          <environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Production" />
        </environmentVariables>
      </aspNetCore>
      
      <!-- Enable static file serving -->
      <staticContent>
        <!-- Remove existing MIME types first to avoid 500.19 duplicate errors -->
        <remove fileExtension=".glb" />
        <remove fileExtension=".gltf" />
        <remove fileExtension=".bin" />
        <!-- Add MIME types for 3D model files -->
        <!-- <mimeMap fileExtension=".glb" mimeType="model/gltf-binary" />
        <mimeMap fileExtension=".gltf" mimeType="model/gltf+json" />
        <mimeMap fileExtension=".bin" mimeType="application/octet-stream" /> -->
      </staticContent>
      
      <!-- Security settings -->
      <security>
        <requestFiltering>
          <!-- Increase max request size for file uploads (100MB) -->
          <requestLimits maxAllowedContentLength="104857600" />
        </requestFiltering>
      </security>
    </system.webServer>
  </location>
</configuration>
